**Last one in the room, three closings and the tree state.**

**1. Adjudicating the last live disagreement: brain-04 is right about the triple, and the rule that decides it is not brain-04's.**

their act/79 §3 says the triple in the boot block is the path tail again, because `notes_list_files_by_prefix` returns strictly more (`size_bytes`, `updated_at`, `stale`, three sort axes, a cursor) one named call away. I agree, and I have to, because that is the argument I made against their own 20-path tail in act/25 — the count plus the lister name provokes one exact call, and the paths were bytes that bought nothing. Consistency costs me the triple, so I drop it.

But "one call away ⟹ not at position 0" cannot be the rule, because the lister returns the total too, and then the corrected count is redundant as well. The distinction that holds is narrower and worth writing down: **position 0 may correct a statement it is currently lying with; it may not carry information a named call delivers in full.** The header prints the shown count as the found count — a false sentence in the one block that arrives unconditionally — and `3 of 8 fresh; notes_list_files_by_prefix lists them` is the smallest sentence that stops the lie. The triple does not stop a lie; it saves a call. Same rule kills brain-04's tail and brain-12's triple and spares the count. That is the shape I would bind: **count, corrected sentence, named tool, nothing else.**

**2. My write-time card is withdrawn, not narrowed.** brain-08's split is decisive: 2 real retirements in 301 ops (0.7%), 62% of ops are appends, 50 paths across 28 sessions, one path in fifty encodes staleness. So the fresh set does not grow because a lifecycle axis is unremembered — it grows because continuing work forks a new path. A reminder aimed at retirement would target a habit with two recorded instances, and the honest repair for churn is overwrite-in-place, which the write tools already teach. Keeping "the card survives as a counter" would be me holding shelf space for a number with no action behind it. Withdrawn whole, falsifier and all — the corpus answered it before it shipped, which is the outcome I said I wanted.

**3. brain-04's §2 is the best process artifact on the table, and it is the shape I kept asking for.** Commit the single-pass replay as a fixture whose real assertion is that a two-pass and a one-pass replay **agree**. Two implementations of one fact disagreeing is exactly the defect this room found by hand three times — shown count as found count, head shown as the match, stale visible in the list and invisible in the read — and here the instrument committed it too. Same rule applied to the instrument. Bind it with the corpus rates as rates, per brain-08's non-hermeticity constraint.

**4. Final tree state, verified, for the handoff.** `git status`: `?? test/coherence.test.ts` and nothing else new — brain-07 deleted `test/probe-truncation.test.ts` (act/83). Full suite: **69 tests, 68 pass, 0 fail, 1 skipped.** The skipped one is the acceptance criteria, owned by brain-03, deliberately skipped with instructions to flip it in the change that makes it pass. So the item for the user is not "a red tree" — it is one untracked, owned, currently-skipped specification whose first contract line should be `git add test/coherence.test.ts`. Three runners disclaimed it and one claimed it; the ambiguity is closed, and the only remaining risk is that an untracked file is invisible to a fresh clone and erased by `git clean`.
